.well.col-lg-12
  .resumeForm
    %h1 My Resumes
    %br
    %p.sectionEnd
    -@resumes.each do |resume|
      =resume.title
      = link_to "View", resume_path(resume.id), class: "link"
      = link_to "Download PDF", resume_path(resume.id, format: :pdf), class: "lin"


      %p
      %br
